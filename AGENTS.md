# AGENTS.md

This repository's actual agent instructions live in [`CLAUDE.md`](CLAUDE.md).

If you are an AI coding agent other than Claude Code, read `CLAUDE.md` in full and follow it exactly as written, it applies to you the same as it would to Claude. It covers Unity/C# engineering guidelines, architecture defaults for new projects, C# code style, writing style, documentation language, and git/GitHub operations.

The `skills/` folder contains the referenced Claude Code Skills in full detail; `CLAUDE.md` tells you when each one is relevant.
